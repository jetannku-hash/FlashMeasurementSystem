# Decisions (from ADRs)

Synthesized by gsd-doc-synthesizer. Mode: new. Precedence: ADR > SPEC > PRD > DOC.

No ADR-typed documents were present in the ingest set (0 of 17). Therefore no
architecture decision records were extracted and no LOCKED decisions exist.

Note: `docs/ROADMAP_待辦與決策.md` (typed SPEC by authoritative manifest) contains
an embedded "deferred-decision log" with records that resemble ADRs (§5 decision
entries). Because the document was not typed ADR, those entries were NOT promoted
to bindable/lockable decisions here — they are captured verbatim as context in
`constraints.md` (roadmap entry) and `context.md`. If any of those should govern
downstream planning as hard decisions, re-tag the source as ADR via `--manifest`
and re-run ingest. See INFO-3 in `../INGEST-CONFLICTS.md`.
