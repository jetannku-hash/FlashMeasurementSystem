# ── Build outputs ──────────────────────────────
bin/
obj/

# ── Visual Studio ──────────────────────────────
.vs/
*.user
*.suo
*.userosscache
*.sln.docstates

# ── Quarantined backup (見 CLAUDE.md：保留勿動、不納入版控) ──
_obsolete_build_20260605/

# ── Generated knowledge graph (graphify 產生物) ──
graphify-out/

# ── Data: 輸出 / 大型 / 機器專屬（見 CLAUDE.md 與全域規範） ──
data/logs/
data/images/
data/reports/
data/templates/*.shm

# ── NuGet 還原資料夾（本專案改用 vendored DLL，不使用 packages/） ──
# 注意：lib/ 下的 vendored 第三方 DLL（如 Newtonsoft.Json）是「刻意提交」的依賴，
# 非 build 產物，請勿加入忽略——否則其他機器以 dotnet build 會找不到引用。
packages/

# ── Misc ──────────────────────────────
*.tmp
Thumbs.db
